// Modelo criado para evitar tipos repetidos em diferentes arquivos.
// Este tipo representa o produto já adaptado para uso interno na loja.
export type ProdutoLoja = {
  nome: string;
  preco: number;
};
